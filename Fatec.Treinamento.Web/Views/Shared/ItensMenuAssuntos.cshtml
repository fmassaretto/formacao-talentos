@model IEnumerable<Fatec.Treinamento.Model.DTO.CursosPorAssunto>

<li>@Html.ActionLink("Todos os cursos", "Index", "Curso")</li>
<li role="separator" class="divider"></li>
<li class="dropdown-header">ASSUNTOS</li>
@foreach (var item in Model)
{
    <li>@Html.ActionLink(item.NomeAssunto + " (" + item.TotalCursos + ")", "Assunto", "Curso", new { Id = item.IdCurso }, null) </li>
}

